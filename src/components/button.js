import React from 'react'

function Button() {
  return (
    <div>
        <button onClick={alert}>Click me</button>
        <button onClick={alert}>Click me</button>
        <button onClick={alert}>Click me</button>
    </div>
  )
}

export default Button